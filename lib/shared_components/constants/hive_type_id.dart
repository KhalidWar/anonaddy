import 'package:hive/hive.dart';

/// These Ids are used to generate TypeAdapter for objects/models
/// by annotating them with  @[HiveType]
class HiveTypeId {
  static const alias = 0;
  static const recipient = 1;
  static const profile = 2;
}
